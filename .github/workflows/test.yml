name: Test
on:
  pull_request:
    branches:
    - main
    types: ["opened", "synchronized", "reopened", "labeled", "ready_for_review", "review_request_removed"]
  pull_request_review:
    branches:
    - main

jobs:
  test:
    runs-on: [ubuntu-latest]
    steps:
    - name: Check-out code
      uses: actions/checkout@v2
    - uses: antoninbas/review-manager@main
      with:
        area_ownership_file: 'AREA-OWNERS'
